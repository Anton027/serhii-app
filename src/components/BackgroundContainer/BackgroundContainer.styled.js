import styled from '@emotion/styled';
import image from '../../images/VectorBlack.png'

export const BackgroundWrap = styled.div`
    background-image: url(${image});
    background-repeat: no-repeat;
    background-size: contain;
    object-fit: cover;
	
`